// Write an anonymous function that calculates BMI (Body Mass Index).
// Formula: BMI = weight / (height * height)

const bmiResult = function (weight, height) {
  BMI = weight / (height * height);
  
 return BMI;
};

console.log(bmiResult(50, 5))